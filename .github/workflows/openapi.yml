name: Open API deployment

on: [push, pull_request]

jobs:
  lint-api:
    runs-on: ubuntu-latest
    steps:
    - id: step1
      run: ls -la

  deploy-api-spec:
    needs: lint-api
    runs-on: ubuntu-latest
    steps:
    - id: step1
      run: echo "Deploy api spec"

  generate-api-clients:
    needs: lint-api
    runs-on: ubuntu-latest
    steps:
      - id: generate-php-client
        run: echo "Generate PHP Client"